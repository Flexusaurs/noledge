# noledge

noledge is tiny, sleek ZK (zero knowledge) rollup node compatible with Ethereum and multiple hashing algorithm compatibility
built (almost) entirely with Go, based on a Merkle state tree and a lightweight contract.
